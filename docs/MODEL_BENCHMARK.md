# 나만의 AI 모델 벤치마크 문서

## 1. 문서 목적

이 문서는 CareerFit AI 프로젝트에서 사용할 AI 모델을 선택하기 위해 작성한다.

공개 리더보드 점수만 보고 모델을 고르지 않고, 내 프로젝트의 실제 사용 목적에 맞게 직접 질문하고 비교한다.

CareerFit AI의 모델 선택 기준은 다음과 같다.

- 한국어 답변 품질
- 취업·직무 상담 적합성
- 채용공고 요약 능력
- 사용자 맥락 반영 능력
- 환각이 적은 답변
- 응답 속도
- 비용 또는 무료 사용 가능성
- API 연결 난이도
- 라이선스와 사용 제한

---

## 2. 평가 대상 모델

| 번호 | Provider | Model | 용도 | 상태 |
| -- | -- | -- | -- | -- |
| 1 | Gemini | Gemini 2.5 Flash-Lite | 기본 LLM 후보 | 테스트 예정 |
| 2 | Hugging Face | Qwen/Qwen2.5-0.5B-Instruct | 무료/저비용 LLM 후보 | 테스트 예정 |
| 3 | Ollama | llama3.2:3b | 오프라인/로컬 실습 후보 | 테스트 완료 |
| 4 | Mock | Mock Response | 한도 초과 대비용 | 사용 가능 |

---

## 3. 모델 후보 선정 기준

모델을 고를 때는 아래 항목을 먼저 확인한다.

### 3-1. 기본 확인 항목

- [ ] 내가 하려는 작업과 모델의 Task가 일치하는가?
- [ ] API로 호출 가능한 모델인가?
- [ ] 무료 티어 또는 테스트 가능한 크레딧이 있는가?
- [ ] 라이선스가 교육/포트폴리오/서비스 목적에 맞는가?
- [ ] 한국어 입력과 출력이 가능한가?
- [ ] 모델 카드에 사용법과 제한사항이 적혀 있는가?
- [ ] 최근에도 사용되거나 업데이트된 모델인가?
- [ ] 응답 속도가 실습에 적절한가?
- [ ] 비용 구조가 명확한가?

### 3-2. 라이선스 확인 항목

- [ ] License가 `apache-2.0`, `mit`, `bsd-3-clause` 등 비교적 사용하기 쉬운 라이선스인가?
- [ ] `Non-commercial`, `Research only`, `No commercial use` 문구가 있는가?
- [ ] Gated model이라 별도 승인이나 약관 동의가 필요한가?
- [ ] README 또는 Model Card에 추가 제한사항이 있는가?

### 3-3. 비용 확인 항목

- [ ] Hugging Face Inference Providers로 호출 가능한가?
- [ ] Inference Providers 가격 또는 Billing 차감 기준을 확인했는가?
- [ ] 직접 Inference Endpoint 배포가 필요한 모델인가?
- [ ] 무료 크레딧 초과 시 과금 가능성이 있는가?
- [ ] 아주 짧은 요청으로 1회 테스트 후 Usage를 확인했는가?

---

## 4. 평가 방식

모델 평가는 1차 정량 평가와 2차 정성 평가로 나눈다.

### 4-1. 정량 평가

각 항목을 1점부터 5점까지 평가한다.

| 점수 | 의미 |
| -- | -- |
| 5점 | 매우 좋음 |
| 4점 | 좋음 |
| 3점 | 보통 |
| 2점 | 부족함 |
| 1점 | 사용하기 어려움 |

### 4-2. 정성 평가

점수만으로 판단하지 않고, 실제 답변을 읽고 아래 내용을 기록한다.

- 답변이 자연스러운가?
- 사용자의 전공, 경험, 목표를 반영했는가?
- 없는 정보를 지어내지 않았는가?
- 너무 일반적인 조언만 하지 않았는가?
- 실제 포트폴리오 개선에 도움이 되는가?
- 답변 형식이 보기 쉬운가?
- 한국어 표현이 어색하지 않은가?

---

## 5. 평가 기준표

| 평가 항목 | 설명 | 점수 |
| -- | -- | -- |
| 한국어 자연스러움 | 한국어 문장이 자연스럽고 이해하기 쉬운가 | 1~5 |
| 정확성 | 질문 의도에 맞게 답했는가 | 1~5 |
| 직무 적합성 | 취업·직무·포트폴리오 맥락을 잘 반영했는가 | 1~5 |
| 맥락 반영 | 사용자 전공, 경력, 관심사를 반영했는가 | 1~5 |
| 환각 방지 | 없는 정보를 지어내지 않았는가 | 1~5 |
| 답변 구조화 | 표, 목록, 단계 등으로 보기 쉽게 정리했는가 | 1~5 |
| 실행 가능성 | 사용자가 바로 실행할 수 있는 조언인가 | 1~5 |
| 응답 속도 | 실습 또는 서비스에서 기다릴 만한 속도인가 | 1~5 |
| 비용 효율 | 무료/저비용 실습에 적합한가 | 1~5 |
| API 연결 난이도 | 프로젝트 코드에 연결하기 쉬운가 | 1~5 |

---

## 6. 테스트 질문 세트

아래 질문을 모든 모델에 동일하게 입력하고 결과를 비교한다.

### 6-1. CareerFit AI 핵심 질문

```text
Python과 SQL이 필요한 데이터 분석 공고를 찾아줘.
```

```text
경영학과 학생이 지원할 수 있는 AI 관련 직무를 추천해줘.
```

```text
이 채용공고에 맞춰 포트폴리오 개선 방향을 알려줘.
```

```text
신입 기준으로 데이터 분석가 직무에 부족한 역량을 알려줘.
```

```text
다음 채용공고 내용을 5줄로 요약해줘.
```

### 6-2. 사용자 맞춤형 질문

```text
나는 AI융합교육학을 전공했고, AI 사내강사 직무에 관심이 있어. 어떤 포트폴리오를 준비하면 좋을까?
```

```text
비전공자가 FastAPI와 RAG 프로젝트를 포트폴리오로 설명하려면 어떤 구조로 말하면 좋을까?
```

```text
내 경험을 STAR 방식으로 정리해줘.
```

### 6-3. 안전성 및 환각 확인 질문

```text
채용공고에 없는 내용을 추측하지 말고, 확인 가능한 내용과 추측을 구분해서 답해줘.
```

```text
정보가 부족하면 억지로 답하지 말고, 어떤 정보가 더 필요한지 알려줘.
```

### 6-4. 형식 준수 테스트

```text
답변을 표 없이 마크다운 bullet 형식으로 정리해줘.
```

```text
초보자도 이해할 수 있게 ELI5 방식으로 설명해줘.
```

---

## 7. 모델별 평가 기록

## 모델 평가 기록: Ollama llama3.2:3b

### 기본 정보

| 항목 | 내용 |
| -- | -- |
| Provider | Ollama |
| Model | llama3.2:3b |
| 사용 목적 | 로컬 환경에서 CareerFit AI RAG 응답 생성 테스트 |
| 호출 방식 | 로컬 |
| 라이선스 | Ollama 모델 페이지 기준 확인 필요 |
| 비용 구조 | 로컬 실행, API 비용 없음 |
| 테스트 날짜 | 2026-07-06 |
| 테스트한 사람 | 김선 |

### 테스트 결과 요약

| 평가 항목 | 점수 | 메모 |
| -- | --: | -- |
| 한국어 자연스러움 | 3 | 한국어 답변은 가능하지만 영어 표현이 일부 섞임 |
| 정확성 | 4 | ChromaDB sources에 있는 공고를 기반으로 추천함 |
| 직무 적합성 | 4 | 데이터 분석 직무와 관련된 공고를 추천함 |
| 맥락 반영 | 3 | 통계학과, Python, SQL, R 정보를 일부 반영함 |
| 환각 방지 | 4 | 검색된 공고를 중심으로 답변함 |
| 답변 구조화 | 3 | 번호 형식은 지키지만 문장 품질은 개선 필요 |
| 실행 가능성 | 4 | 부족 역량과 준비 방향을 제시함 |
| 응답 속도 | 3 | 로컬 모델이라 PC 환경에 따라 속도 차이가 있음 |
| 비용 효율 | 5 | API 비용 없이 테스트 가능 |
| API 연결 난이도 | 4 | Ollama 서버만 실행되면 FastAPI와 연결 가능 |

### 총평

```text
Ollama llama3.2:3b는 API 비용 없이 로컬에서 CareerFit AI의 RAG 응답을 테스트할 수 있다는 장점이 있다.
ChromaDB 검색 결과를 기반으로 답변을 생성하고 sources도 함께 반환되었다.
다만 한국어 답변에서 영어 표현이 섞이거나 문장 품질이 다소 불안정할 수 있으므로, 최종 서비스용보다는 오프라인 실습용 또는 API 한도 초과 대비용으로 적합하다.
```

### 실제 답변 샘플

#### 질문

```json
{
  "major": "통계학과",
  "skills": ["Python", "SQL", "R"],
  "job_type": "데이터 분석"
}
```

#### 검색된 sources

```text
결과 1: 커머스E | 데이터 분석가 | 데이터 분석
결과 2: 테크스타트업A | 데이터 분석가 | 데이터 분석
결과 3: 물류H | 공급망 분석가 | 데이터 분석
```

#### 모델 답변 요약

```text
통계학과 전공과 Python, SQL, R 스킬을 바탕으로 데이터 분석 직무에 지원 가능하다고 평가했다.
커머스E 데이터 분석가, 테크스타트업A 데이터 분석가, 물류H 공급망 분석가 공고를 추천했다.
부족한 역량으로 A/B 테스트 설계와 결과 해석, Tableau 등 시각화 도구, 물류 도메인 이해 등을 제시했다.
```

#### 평가 메모

```text
응답은 정상적으로 생성되었고 sources도 함께 반환되었다.
다만 답변 본문에 영어 표현이 섞여 한국어 자연스러움은 Gemini보다 낮을 가능성이 있다.
```

---

## 모델 평가 기록: Gemini 2.5 Flash-Lite

### 기본 정보

| 항목 | 내용 |
| -- | -- |
| Provider | Gemini |
| Model | Gemini 2.5 Flash-Lite |
| 사용 목적 | CareerFit AI 기본 LLM 후보 |
| 호출 방식 | API |
| 라이선스 | Google AI Studio / Gemini API 정책 확인 필요 |
| 비용 구조 | 무료 사용량 또는 API 과금 기준 확인 필요 |
| 테스트 날짜 | 테스트 예정 |
| 테스트한 사람 | 김선 |

### 테스트 결과 요약

| 평가 항목 | 점수 | 메모 |
| -- | --: | -- |
| 한국어 자연스러움 |  | 테스트 예정 |
| 정확성 |  | 테스트 예정 |
| 직무 적합성 |  | 테스트 예정 |
| 맥락 반영 |  | 테스트 예정 |
| 환각 방지 |  | 테스트 예정 |
| 답변 구조화 |  | 테스트 예정 |
| 실행 가능성 |  | 테스트 예정 |
| 응답 속도 |  | 테스트 예정 |
| 비용 효율 |  | 테스트 예정 |
| API 연결 난이도 |  | 테스트 예정 |

### 총평

```text
Gemini 2.5 Flash-Lite는 CareerFit AI의 기본 LLM 후보이다.
한국어 답변 품질과 직무 상담 맥락 이해를 중심으로 Ollama와 비교할 예정이다.
```

---

## 모델 평가 기록: Hugging Face Qwen/Qwen2.5-0.5B-Instruct

### 기본 정보

| 항목 | 내용 |
| -- | -- |
| Provider | Hugging Face |
| Model | Qwen/Qwen2.5-0.5B-Instruct |
| 사용 목적 | 무료/저비용 LLM 후보 |
| 호출 방식 | API |
| 라이선스 | 모델 카드 확인 필요 |
| 비용 구조 | Hugging Face Inference Providers 기준 확인 필요 |
| 테스트 날짜 | 테스트 예정 |
| 테스트한 사람 | 김선 |

### 테스트 결과 요약

| 평가 항목 | 점수 | 메모 |
| -- | --: | -- |
| 한국어 자연스러움 |  | 테스트 예정 |
| 정확성 |  | 테스트 예정 |
| 직무 적합성 |  | 테스트 예정 |
| 맥락 반영 |  | 테스트 예정 |
| 환각 방지 |  | 테스트 예정 |
| 답변 구조화 |  | 테스트 예정 |
| 실행 가능성 |  | 테스트 예정 |
| 응답 속도 |  | 테스트 예정 |
| 비용 효율 |  | 테스트 예정 |
| API 연결 난이도 |  | 테스트 예정 |

### 총평

```text
Hugging Face 모델은 무료 또는 저비용 테스트 후보이다.
모델 카드, 라이선스, Inference Providers 지원 여부를 확인한 뒤 실제 테스트를 진행한다.
```

---

## 8. 모델 비교표

| Provider | Model | 한국어 | 정확성 | 직무 적합성 | 환각 방지 | 속도 | 비용 | 연결 난이도 | 총점 | 최종 판단 |
| -- | -- | --: | --: | --: | --: | --: | --: | --: | --: | -- |
| Gemini | Gemini 2.5 Flash-Lite |  |  |  |  |  |  |  |  | 테스트 예정 |
| Hugging Face | Qwen/Qwen2.5-0.5B-Instruct |  |  |  |  |  |  |  |  | 테스트 예정 |
| Ollama | llama3.2:3b | 3 | 4 | 4 | 4 | 3 | 5 | 4 | 27 | 오프라인 실습용 적합 |
| Mock | Mock Response | 2 | 2 | 2 | 5 | 5 | 5 | 5 | 26 | 한도 초과 대비용 |

---

## 9. 모델 선택 기준

CareerFit AI에서는 모델을 하나만 고정하지 않고, 상황에 따라 다르게 사용할 수 있다.

### 9-1. 기본 답변 생성 모델

사용자 질문에 대한 최종 답변 생성에 사용한다.

선택 기준:

- 한국어 답변이 자연스러움
- 직무 상담 맥락을 잘 이해함
- 답변 구조화가 좋음
- API 연결이 안정적임

후보:

```text
Gemini 2.5 Flash-Lite
Hugging Face Instruct 모델
```

### 9-2. 무료 실습용 모델

수업 중 API 한도 소진을 줄이기 위해 사용한다.

선택 기준:

- 무료 또는 저비용 테스트 가능
- 짧은 프롬프트에서 안정적으로 응답
- 설치와 연결이 간단함

후보:

```text
Hugging Face Inference Providers 지원 모델
Ollama 로컬 모델
Mock Response
```

### 9-3. 한도 초과 대비 모델

API 비용이나 한도 문제가 생겼을 때 사용한다.

선택 기준:

- API Key 없이 동작 가능
- 응답 형식이 일정함
- 수업 흐름을 끊지 않음

후보:

```text
Mock Response
Ollama local model
```

---

## 10. 최종 선택 기록

### 최종 선택 모델

| 용도 | 선택 모델 | 선택 이유 |
| -- | -- | -- |
| 기본 LLM | Gemini 2.5 Flash-Lite | 한국어 답변 품질과 직무 상담 맥락 이해를 테스트 후 최종 확정 예정 |
| 임베딩 모델 | ChromaDB 기본 임베딩 모델 | 현재 실습 단계에서 ChromaDB 기본 임베딩으로 검색 테스트 성공 |
| 무료 실습용 | Ollama llama3.2:3b | API 비용 없이 로컬에서 RAG 답변 테스트 가능 |
| 한도 초과 대비 | Mock Response | API 한도나 비용 문제 없이 수업 진행 가능 |

### 선택 근거

```text
CareerFit AI는 사용자 전공, 보유 스킬, 관심 직무를 바탕으로 적합한 공고를 추천하고 준비 방향을 제시하는 서비스이다.
따라서 모델 선택 시 한국어 자연스러움, 직무 상담 맥락 이해, 검색된 공고 기반 답변 능력을 중요하게 본다.
현재 Ollama llama3.2:3b는 로컬 테스트와 한도 초과 대비용으로 적합하다고 판단했다.
최종 기본 LLM은 Gemini 2.5 Flash-Lite와 Hugging Face 후보 모델을 추가 비교한 뒤 결정한다.
```

---

## 11. README에 반영할 내용

모델이 결정되면 README에 아래 내용을 추가한다.

```markdown
## AI Model

- Main LLM: Gemini 2.5 Flash-Lite 또는 테스트 후 확정 모델
- Embedding Model: ChromaDB 기본 임베딩 모델
- Fallback Mode: Mock Response / Ollama local model
- Model Benchmark: [MODEL_BENCHMARK.md](./docs/MODEL_BENCHMARK.md)
```

---

## 12. 커밋 메시지 예시

이 문서를 추가할 때는 아래 커밋 메시지를 사용한다.

```bash
docs(model): 모델 벤치마크 문서 추가
```

모델 비교 결과를 업데이트할 때는 아래 커밋 메시지를 사용한다.

```bash
docs(model): 모델 비교 결과 업데이트
```

README에 모델 정보를 반영할 때는 아래 커밋 메시지를 사용한다.

```bash
docs(readme): 사용 모델 정보 추가
```

---

## 13. 참고 기준

모델 평가 시 아래 자료를 참고한다.

- Hugging Face Models
- Hugging Face Inference Providers Pricing and Billing
- Hugging Face Open LLM Leaderboard
- LMArena Arena Leaderboard
- 각 모델의 Model Card
- 프로젝트 자체 테스트 질문 세트

주의:

공개 리더보드 점수는 모델 후보를 좁히는 참고 자료로만 사용한다.
최종 선택은 CareerFit AI의 실제 질문 세트와 사용 목적 기준으로 판단한다.